export enum Environments {
  PRODUCTION = 'production',
  DEV = 'development',
  TEST = 'test',
  STAGING = 'staging',
}

export enum EnvironmentFile {
  DEFAULT = '.env',
  PRODUCTION = '.env',
  DEV = '.env',
  TEST = '.env',
  STAGING = '.env',
}
